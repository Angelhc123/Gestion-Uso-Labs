import 'package:flutter/material.dart';

// Colores Comunes de la Aplicación
const Color primaryDarkPurple = Color(0xFF381E72);
const Color secondaryDark = Color(0xFF1C1B1F); // Fondo principal oscuro
const Color accentPurple = Color(0xFFD0BCFF); // Acento principal
const Color textOnDark = Colors.white;
const Color textOnDarkSecondary = Color(0xFFCAC4D0); // Texto secundario o hints

// Colores de Estado
const Color errorColor = Colors.redAccent;
const Color successColor = Colors.greenAccent;
const Color warningColor = Colors.orangeAccent;

// Colores Específicos (si fueran necesarios para secciones particulares)
// Ejemplo: Colores para el panel de Admin si difieren significativamente
const Color adminViewSecondaryDark = Color(0xFF242424); // Un fondo ligeramente diferente para admin
const Color adminViewAccentPurple = Color(0xFF9B59B6); // Un acento diferente para admin
